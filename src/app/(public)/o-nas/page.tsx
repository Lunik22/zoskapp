// src/app/o-mne/page.tsx

import AboutView from '@/sections/AboutView';

export const metadata = { title: `O mne | ZoškApp` };

export default function About() {

  return (
    <AboutView />
  );
}
